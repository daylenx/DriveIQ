/**
 * Feature Flags for DriveIQ
 * 
 * These flags control feature availability during development.
 * Set PAYWALLS_ENABLED = true when ready for production payments.
 */

export const PAYWALLS_ENABLED = false;

export const DEV_PREVIEW_PAYWALL = true;
